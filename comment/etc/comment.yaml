grpc:
  mode: dev
  name: passport-grpc
  listenon: 0.0.0.0:6667
  log:
    level: debug

mysql:
  user: ${ENV_DB_USER}
  pass: ${ENV_DB_PASS}
  addr: ${ENV_DB_ADDR}
  db_name: ${ENV_DB_NAME}

# 外部依赖
external:
  grpc:
    passport: 127.0.0.1:7778
    seqer: 127.0.0.1:9528
