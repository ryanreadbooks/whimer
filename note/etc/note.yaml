http:
  mode: dev
  name: note-api
  host: 0.0.0.0
  port: 8888
  log:
    level: debug

mysql:
  user: ${ENV_DB_USER}
  pass: ${ENV_DB_PASS}
  addr: ${ENV_DB_ADDR}
  db_name: ${ENV_DB_NAME}

oss:
  user: ${ENV_OSS_USER}
  pass: ${ENV_OSS_PASS}
  endpoint: localhost:9000
  location: local
  bucket: nota
  prefix: assets

3rd:
  grpc:
    access: 127.0.0.1:7778
