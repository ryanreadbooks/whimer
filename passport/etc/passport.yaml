name: passport-api
host: 0.0.0.0
port: 7777
log:
  level: debug

mysql:
  user: ${ENV_DB_USER}
  pass: ${ENV_DB_PASS}
  addr: ${ENV_DB_ADDR}
  db_name: ${ENV_DB_NAME}

redis:
  host: localhost:7542

oss:
  user: ${ENV_OSS_USER}
  pass: ${ENV_OSS_PASS}
  endpoint: 127.0.0.1:9000
  location: local
  bucket: passport
  prefix: avatars

idgen:
  addr: 127.0.0.1:9528
